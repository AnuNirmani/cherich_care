import 'package:flutter/material.dart';

class TheamColor extends StatelessWidget{
  const TheamColor({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
    );
  }
}