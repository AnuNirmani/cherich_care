import 'package:flutter/material.dart';

class FeedbackP extends StatelessWidget{
  const FeedbackP({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
    );
  }
}